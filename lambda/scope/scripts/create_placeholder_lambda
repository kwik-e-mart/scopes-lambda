#!/bin/bash
# Create the placeholder Lambda function using a pre-built public container image.
# The image is published once via lambda/scope/placeholder/publish and configured
# via PLACEHOLDER_IMAGE_URI in values.yaml.

source "$SERVICE_PATH/utils/log"

log info "🔍 Creating placeholder Lambda function (container image)..."

if [ -z "${LAMBDA_FUNCTION_NAME:-}" ]; then
  echo "❌ LAMBDA_FUNCTION_NAME is not set" >&2
  echo "💡 Possible causes:" >&2
  echo "   - build_context did not compute the function name" >&2
  echo "🔧 How to fix:" >&2
  echo "   - Ensure build_context runs before this script" >&2
  return 1
fi

if [ -z "${LAMBDA_ROLE_ARN:-}" ]; then
  echo "❌ LAMBDA_ROLE_ARN is not set" >&2
  echo "💡 Possible causes:" >&2
  echo "   - create_iam_role did not run or failed" >&2
  echo "🔧 How to fix:" >&2
  echo "   - Ensure create_iam_role runs before this script" >&2
  return 1
fi

if [ -z "${OUTPUT_DIR:-}" ]; then
  echo "❌ OUTPUT_DIR is not set" >&2
  echo "💡 Possible causes:" >&2
  echo "   - The scope runner did not set a working directory" >&2
  return 1
fi

placeholder_image_uri="${PLACEHOLDER_IMAGE_URI:-public.ecr.aws/nullplatform/aws-lambda/nullplatform-lambda-placeholder:latest}"
architecture="${ARCHITECTURE:-arm64}"

log debug "   📋 function_name=$LAMBDA_FUNCTION_NAME"
log debug "   📋 role_arn=$LAMBDA_ROLE_ARN"
log debug "   📋 architecture=$architecture"
log debug "   📋 placeholder_image_uri=$placeholder_image_uri"

# Check if function already exists
echo ""
log info "   📡 Checking if function already exists..."

existing_function=$(aws lambda get-function \
  --function-name "$LAMBDA_FUNCTION_NAME" \
  2>&1 || echo "NOT_FOUND")

if ! echo "$existing_function" | grep -q "ResourceNotFoundException\|NOT_FOUND"; then
  log info "   ✅ Function already exists - skipping creation"

  LAMBDA_FUNCTION_ARN=$(echo "$existing_function" | jq -r '.Configuration.FunctionArn')
  export LAMBDA_FUNCTION_ARN

  log debug "   📋 function_arn=$LAMBDA_FUNCTION_ARN"
  return 0
fi

# Validate placeholder image is reachable in public ECR
# Parses the URI to extract path (alias/repo) and tag, then checks the manifest
# via the public ECR OCI registry API - no Docker or AWS credentials required.
echo ""
log info "   📡 Validating placeholder image in public ECR..."

image_ref="${placeholder_image_uri#public.ecr.aws/}"   # strip host

if [[ "$image_ref" == *":"* ]]; then
  image_tag="${image_ref##*:}"
  image_path="${image_ref%:*}"
else
  image_tag="latest"
  image_path="$image_ref"
fi

token_response=$(curl -sfL \
  "https://public.ecr.aws/token/?scope=repository:${image_path}:pull&service=public.ecr.aws" \
  2>/dev/null || echo "")

token=""
if [ -n "$token_response" ]; then
  token=$(echo "$token_response" | jq -r '.token // empty' 2>/dev/null || echo "")
fi

if [ -z "$token" ]; then
  echo "" >&2
  echo "❌ Cannot reach public ECR to validate placeholder image" >&2
  echo "" >&2
  echo "💡 Possible causes:" >&2
  echo "   • The agent has no outbound internet access" >&2
  echo "   • public.ecr.aws is temporarily unavailable" >&2
  echo "" >&2
  echo "🔧 How to fix:" >&2
  echo "   • Verify outbound connectivity: curl -s https://public.ecr.aws" >&2
  return 1
fi

http_code=$(curl -s -o /dev/null -w "%{http_code}" \
  -H "Authorization: Bearer $token" \
  -H "Accept: application/vnd.oci.image.index.v1+json, application/vnd.docker.distribution.manifest.v2+json" \
  "https://public.ecr.aws/v2/${image_path}/manifests/${image_tag}")

if [ "$http_code" != "200" ]; then
  echo "" >&2
  echo "❌ Placeholder image not found: $placeholder_image_uri (HTTP $http_code)" >&2
  echo "" >&2
  echo "💡 Possible causes:" >&2
  echo "   • The image has not been published yet" >&2
  echo "   • PLACEHOLDER_IMAGE_URI in values.yaml points to a wrong tag or path" >&2
  echo "" >&2
  echo "🔧 How to fix:" >&2
  echo "   • Publish the image once by running: lambda/scope/placeholder/publish" >&2
  echo "   • Verify PLACEHOLDER_IMAGE_URI=$placeholder_image_uri is correct" >&2
  return 1
fi

log debug "   ✅ Placeholder image found"

# Build tags argument for AWS CLI
tags_arg=""
if [ -n "${RESOURCE_TAGS_JSON:-}" ]; then
  tags_arg=$(echo "$RESOURCE_TAGS_JSON" | jq -r 'to_entries | map("\(.key)=\(.value)") | join(",")')
fi

# Create the Lambda function with the pre-built placeholder image
echo ""
log info "   📡 Creating Lambda function..."

create_output=$(aws lambda create-function \
  --function-name "$LAMBDA_FUNCTION_NAME" \
  --package-type Image \
  --code "ImageUri=$placeholder_image_uri" \
  --architectures "$architecture" \
  --role "$LAMBDA_ROLE_ARN" \
  --memory-size 128 \
  --timeout 30 \
  --description "Placeholder Lambda - created by nullplatform" \
  ${tags_arg:+--tags "$tags_arg"} \
  2>&1)
create_exit_code=$?

# Retry on IAM role propagation delay
if [ $create_exit_code -ne 0 ] && \
   echo "$create_output" | grep -q "The role defined for the function cannot be assumed"; then
  log warn "   ⚠️  Waiting for IAM role propagation..."

  max_retries=5
  retry_delay=10

  for i in $(seq 1 $max_retries); do
    log info "   📝 Retry $i/$max_retries (waiting ${retry_delay}s)..."
    sleep "$retry_delay"

    create_output=$(aws lambda create-function \
      --function-name "$LAMBDA_FUNCTION_NAME" \
      --package-type Image \
      --code "ImageUri=$placeholder_image_uri" \
      --architectures "$architecture" \
      --role "$LAMBDA_ROLE_ARN" \
      --memory-size 128 \
      --timeout 30 \
      --description "Placeholder Lambda - created by nullplatform" \
      ${tags_arg:+--tags "$tags_arg"} \
      2>&1)
    create_exit_code=$?

    [ $create_exit_code -eq 0 ] && break
    retry_delay=$((retry_delay + 10))
  done
fi

if ! echo "$create_output" | grep -q "FunctionArn"; then
  echo "" >&2
  echo "❌ Failed to create Lambda function '$LAMBDA_FUNCTION_NAME'" >&2
  echo "" >&2

  if echo "$create_output" | grep -qi "ImageNotFoundException\|invalid.*image\|repository does not exist"; then
    echo "  🖼️  Error: Placeholder image not found or not accessible" >&2
    echo "" >&2
    echo "  💡 Possible causes:" >&2
    echo "    • The placeholder image has not been published yet" >&2
    echo "    • PLACEHOLDER_IMAGE_URI=$placeholder_image_uri is incorrect" >&2
    echo "" >&2
    echo "  🔧 How to fix:" >&2
    echo "    • Publish the image once by running: lambda/scope/placeholder/publish" >&2
  else
    echo "  💡 Possible causes:" >&2
    echo "    • IAM role not yet propagated (even after retries)" >&2
    echo "    • Insufficient permissions to create Lambda functions" >&2
    echo "    • Invalid architecture '$architecture'" >&2
    echo "" >&2
    echo "  🔧 How to fix:" >&2
    echo "    • Verify the agent has lambda:CreateFunction permission" >&2
    echo "    • Check that role_arn=$LAMBDA_ROLE_ARN exists and is assumable by Lambda" >&2
    echo "    • AWS error: $create_output" >&2
  fi

  echo "" >&2
  return 1
fi

LAMBDA_FUNCTION_ARN=$(echo "$create_output" | jq -r '.FunctionArn')
log debug "   ✅ Function created: function_arn=$LAMBDA_FUNCTION_ARN"

# Create main alias pointing to $LATEST
echo ""
log info "   📝 Creating main alias..."

alias_name="${LAMBDA_MAIN_ALIAS_NAME:-main}"

aws lambda create-alias \
  --function-name "$LAMBDA_FUNCTION_NAME" \
  --name "$alias_name" \
  --function-version "\$LATEST" \
  --description "Main alias for traffic routing" \
  >/dev/null 2>&1 || true

log debug "   ✅ Alias '$alias_name' created"

export LAMBDA_FUNCTION_ARN

echo ""
log info "✨ Placeholder Lambda created: function_name=$LAMBDA_FUNCTION_NAME, function_arn=$LAMBDA_FUNCTION_ARN"
