#!/bin/bash
# List available Lambda metrics

cat << 'EOF'
Available Lambda Metrics:

Invocation Metrics:
   - Invocations: Number of function invocations
   - Errors: Number of invocations that resulted in errors
   - Throttles: Number of throttled invocations

Performance Metrics:
   - Duration: Execution time in milliseconds
   - IteratorAge: Age of the last record processed (for stream-based invocations)

Concurrency Metrics:
   - ConcurrentExecutions: Number of concurrent executions
   - ProvisionedConcurrencyInvocations: Invocations using provisioned concurrency
   - ProvisionedConcurrencySpilloverInvocations: Invocations that exceeded provisioned concurrency

Async Metrics:
   - AsyncEventsReceived: Events received for async invocation
   - AsyncEventAge: Age of async events

Use the metric workflow to fetch specific metrics with time ranges.
EOF
